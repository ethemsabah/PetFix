
import 'package:flutter/material.dart';

class ThirdPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Seçenek 3'),
      ),
      body: Center(
        child: Text('Seçenek 3 Sayfası'),
      ),
    );
  }
}
